<?xml version="1.0" encoding="utf-8"?><testsuites name="pytest tests"><testsuite name="pytest" errors="0" failures="2" skipped="0" tests="2" time="0.156" timestamp="2026-07-19T16:24:52.451544+08:00" hostname="baideMacBook-Pro.local"><testcase classname="tests.tools.test_image_generation_plugin_dispatch" name="test_image_provider_boundary_final_reauth_blocks_state_drift_without_io" time="0.025"><failure message="AssertionError: dispatch did not freeze one route decision; state drift reached Provider I/O; state drift did not return a blocked result; state drift did not return stable stale error_code; state drift did not return stable stale error_type&#10;assert ['dispatch di...e error_type'] == []&#10;  &#10;  Left contains 5 more items, first extra item: 'dispatch did not freeze one route decision'&#10;  Use -v to get more diff">monkeypatch = &lt;_pytest.monkeypatch.MonkeyPatch object at 0x10a0242d0&gt;

    def test_image_provider_boundary_final_reauth_blocks_state_drift_without_io(
        monkeypatch,
    ):
        """A route decision must be reauthorized after it is frozen, before Provider I/O."""
        from agent import image_runtime
        from agent import image_gen_registry as registry_module
        from agent.image_gen_verification import ImageGenRequestBinding
        from hermes_cli import plugins as plugins_module
        from tools import image_generation_tool

        decision_factory = getattr(
            image_runtime,
            "build_capability_route_decision",
            None,
        )
        assert callable(decision_factory), "CapabilityRouteDecision factory is missing"

        verified = {
            "schema_version": 1,
            "fingerprint": "image-final-reauth-v1",
            "status": "verified",
            "available": True,
            "reason_code": "ready",
            "provider": "codex",
            "model": "gpt-image-2",
        }
        stale = {
            **verified,
            "status": "configured_unverified",
            "available": False,
            "reason_code": "image_generation_not_verified",
        }
        binding = ImageGenRequestBinding(
            provider="codex",
            model="gpt-image-2",
            api_key="pinned-before-drift",
            runtime_identity={
                "identity_supported": True,
                "endpoint_resolved": True,
                "endpoint": "https://pinned-before-drift.example.test/v1",
            },
        )
        decision = decision_factory(
            "image_generation",
            snapshot=verified,
            route="provider",
            tool_call_id="call-image-final-reauth",
            request_binding=binding,
        )
        runtime = {"snapshot": dict(verified)}
        build_calls = []

        def freeze_then_drift(*args, **kwargs):
            build_calls.append((args, kwargs))
            # The verification state changes after the caller's initial read but
            # before the Provider-boundary final authorization.
            runtime["snapshot"] = dict(stale)
            return decision

        monkeypatch.setattr(
            image_runtime,
            "build_capability_route_decision",
            freeze_then_drift,
        )
        monkeypatch.setattr(
            image_runtime,
            "verification_runtime_snapshot",
            lambda *_args, **_kwargs: dict(runtime["snapshot"]),
        )
        monkeypatch.setattr(
            plugins_module,
            "_ensure_plugins_discovered",
            lambda *args, **kwargs: None,
        )
        provider_io = []

        class BoundaryProvider(ImageGenProvider):
            @property
            def name(self) -&gt; str:
                return "codex"

            def generate(self, prompt, aspect_ratio="landscape", **kwargs):
                provider_io.append(
                    {
                        "prompt": prompt,
                        "aspect_ratio": aspect_ratio,
                        **kwargs,
                    }
                )
                return {
                    "success": True,
                    "provider": "codex",
                    "model": "gpt-image-2",
                    "image": "/tmp/should-not-exist.png",
                }

        provider = BoundaryProvider()
        monkeypatch.setattr(
            registry_module,
            "get_provider",
            lambda name: provider if name == "codex" else None,
        )

        result = json.loads(
            image_generation_tool._handle_image_generate(
                {"prompt": "draw the boundary", "aspect_ratio": "square"},
                caller_capability_fingerprint=verified["fingerprint"],
            )
        )

        violations = []
        if len(build_calls) != 1:
            violations.append("dispatch did not freeze one route decision")
        if provider_io:
            violations.append("state drift reached Provider I/O")
        if result.get("success") is not False:
            violations.append("state drift did not return a blocked result")
        if result.get("error_code") != "capability_caller_stale":
            violations.append("state drift did not return stable stale error_code")
        if result.get("error_type") != "capability_caller_stale":
            violations.append("state drift did not return stable stale error_type")
&gt;       assert violations == [], "; ".join(violations)
E       AssertionError: dispatch did not freeze one route decision; state drift reached Provider I/O; state drift did not return a blocked result; state drift did not return stable stale error_code; state drift did not return stable stale error_type
E       assert ['dispatch di...e error_type'] == []
E
E         Left contains 5 more items, first extra item: 'dispatch did not freeze one route decision'
E         Use -v to get more diff

tests/tools/test_image_generation_plugin_dispatch.py:541: AssertionError</failure></testcase><testcase classname="tests.tools.test_image_generation_plugin_dispatch" name="test_image_dispatch_uses_private_pinned_binding_after_ambient_rotation" time="0.001"><failure message="AssertionError: dispatch did not freeze one route decision; Provider did not receive the private pinned binding&#10;assert ['dispatch di...nned binding'] == []&#10;  &#10;  Left contains 2 more items, first extra item: 'dispatch did not freeze one route decision'&#10;  Use -v to get more diff">monkeypatch = &lt;_pytest.monkeypatch.MonkeyPatch object at 0x10a01bfd0&gt;

    def test_image_dispatch_uses_private_pinned_binding_after_ambient_rotation(
        monkeypatch,
    ):
        """Ambient credentials changed after reauth must not replace the private binding."""
        from agent import image_runtime
        from agent import image_gen_registry as registry_module
        from agent.image_gen_verification import ImageGenRequestBinding
        from hermes_cli import plugins as plugins_module
        from tools import image_generation_tool

        decision_factory = getattr(
            image_runtime,
            "build_capability_route_decision",
            None,
        )
        assert callable(decision_factory), "CapabilityRouteDecision factory is missing"

        verified = {
            "schema_version": 1,
            "fingerprint": "image-pinned-binding-v1",
            "status": "verified",
            "available": True,
            "reason_code": "ready",
            "provider": "codex",
            "model": "gpt-image-2",
        }
        pinned_endpoint = "https://pinned-image.example.test/v1"
        pinned_secret = "pinned-image-secret"
        binding = ImageGenRequestBinding(
            provider="codex",
            model="gpt-image-2",
            api_key=pinned_secret,
            runtime_identity={
                "identity_supported": True,
                "endpoint_resolved": True,
                "endpoint": pinned_endpoint,
            },
        )
        decision = decision_factory(
            "image_generation",
            snapshot=verified,
            route="provider",
            tool_call_id="call-image-pinned-binding",
            request_binding=binding,
        )
        build_calls = []

        def build_decision(*args, **kwargs):
            build_calls.append((args, kwargs))
            return decision

        monkeypatch.setattr(
            image_runtime,
            "build_capability_route_decision",
            build_decision,
        )
        monkeypatch.setattr(
            image_runtime,
            "verification_runtime_snapshot",
            lambda *_args, **_kwargs: dict(verified),
        )
        monkeypatch.setenv("OPENAI_API_KEY", "ambient-before-reauth")
        monkeypatch.setenv(
            "OPENAI_BASE_URL",
            "https://ambient-before-reauth.example.test/v1",
        )
        reauth_calls = []

        def final_reauth(_snapshot):
            reauth_calls.append(True)
            # Authorization has succeeded. Rotate the ambient process state before
            # the Provider consumes its request-local binding.
            monkeypatch.setenv("OPENAI_API_KEY", "ambient-after-reauth")
            monkeypatch.setenv(
                "OPENAI_BASE_URL",
                "https://ambient-after-reauth.example.test/v1",
            )
            return True

        monkeypatch.setattr(
            image_generation_tool,
            "_same_authorization_snapshot",
            final_reauth,
        )
        monkeypatch.setattr(
            plugins_module,
            "_ensure_plugins_discovered",
            lambda *args, **kwargs: None,
        )
        provider_calls = []

        class BindingAwareProvider(ImageGenProvider):
            _supports_pinned_image_request_binding = True

            @property
            def name(self) -&gt; str:
                return "codex"

            def generate(self, prompt, aspect_ratio="landscape", **kwargs):
                provider_calls.append(kwargs)
                return {
                    "success": True,
                    "provider": "codex",
                    "model": kwargs.get("model"),
                    "image": "/tmp/pinned-binding.png",
                }

        provider = BindingAwareProvider()
        monkeypatch.setattr(
            registry_module,
            "get_provider",
            lambda name: provider if name == "codex" else None,
        )

        result = json.loads(
            image_generation_tool._handle_image_generate(
                {"prompt": "draw the pinned route", "aspect_ratio": "portrait"},
                caller_capability_fingerprint=verified["fingerprint"],
            )
        )

        observed_binding = (
            provider_calls[0].get("_runtime_binding")
            if len(provider_calls) == 1
            else None
        )
        violations = []
        if result.get("success") is not True:
            violations.append("pinned image dispatch unexpectedly failed")
        if len(build_calls) != 1:
            violations.append("dispatch did not freeze one route decision")
        if reauth_calls != [True]:
            violations.append("dispatch did not perform exactly one final reauth")
        if len(provider_calls) != 1:
            violations.append("Provider was not called exactly once")
        if observed_binding is not binding:
            violations.append("Provider did not receive the private pinned binding")
        if (
            observed_binding is not None
            and observed_binding.api_key != pinned_secret
        ):
            violations.append("Provider binding secret changed with ambient state")
        if (
            observed_binding is not None
            and observed_binding.runtime_identity["endpoint"] != pinned_endpoint
        ):
            violations.append("Provider binding endpoint changed with ambient state")
        if os.environ["OPENAI_API_KEY"] != "ambient-after-reauth":
            violations.append("test did not rotate the ambient secret after reauth")
        if (
            os.environ["OPENAI_BASE_URL"]
            != "https://ambient-after-reauth.example.test/v1"
        ):
            violations.append("test did not rotate the ambient endpoint after reauth")
&gt;       assert violations == [], "; ".join(violations)
E       AssertionError: dispatch did not freeze one route decision; Provider did not receive the private pinned binding
E       assert ['dispatch di...nned binding'] == []
E
E         Left contains 2 more items, first extra item: 'dispatch did not freeze one route decision'
E         Use -v to get more diff

tests/tools/test_image_generation_plugin_dispatch.py:698: AssertionError</failure></testcase></testsuite></testsuites>