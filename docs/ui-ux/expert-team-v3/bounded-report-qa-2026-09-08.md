# 深度材料研究团收敛优化：前端 UX QA 与执行台账

## 最终续执行状态（覆盖下方过程记录）

收尾授权更新：用户已明确允许使用当前可用模型替代不可用的 Sol 完成本轮最终审核；仅为本轮例外，不修改项目默认规则。政府短流程保留未通过，不再重跑。当前开始精确暂存及独立审核，提交和推送结果以最终 Git 证据为准。

终审一轮修正：首次完整补丁审核发现冻结 writing_contract/单元 budget 未实际传给 Provider，以及无附件粘贴资料 evidence 被提前扩展字段导致严格 schema 拒绝。已补 13 项参数化失败回归并完成最小修复：传递冻结合同及预算；让 source_register/evidence_matrix 使用同一服务端背景 snapshot，删除 runtime 非法五字段改写，不放宽通用 schema。旧 v2 测试同时明确区分冻结 v2 与当前 public v3 目录。固定 Node 24 后标准隔离 pytest 六文件 246 passed（1 条既有 audioop 弃用警告），git diff --check 通过。此前完整验证证明修正前冻结代码；本次新增改动由针对性回归覆盖，未重新跑实际报告。重新暂存后的独立终审结果及提交状态以最终 Git 证据为准。

源码修复及完整离线验证通过，企业工作稿已通过产品导出并在 WPS 完成本机确认；整体任务尚未全部完成。政府加资料不足短流程被资料不匹配阻断，未生成初步稿。工具不支持规定的 Sol 模型，最终审核、commit、push 未执行。

- 本轮代码冻结后的 `scripts/verify.sh --full` 会话 65230 已结束，退出码 0，最终 `verification: PASS`。先前静态 URL 断言失败已修复并由此次完整运行覆盖。
- 企业 attempt-3 的 WPS 首中尾检查及产品本机确认完成，workflow_state 为 `completed`，delivery_gate 为 `passed`，grade 仍为 `quality_review_required`；实际正文 12,126 字超上限，不能称正式内容通过。真实 UI 显示正确字数、等级及 5 项可展开审阅意见。末页留白为 P2 限制。
- blocked 工作稿出口已修复并通过有界回归，审核 ledger/回执身份与工程门禁保留，真实问题及声明绑定 DOCX 指纹。历史无标记任务保持冻结语义。
- 用户明确同意“改为短流程，尽快收尾”：政府与资料不足合并为短流程，完整政府长报告不再执行，标为未验证。
- 短流程 run `et-663cd7e6e1611833d936d1874a0016f7fb249e04a66d83e72f97792e2f29bec2`，session `etl-bb3c7491b8d742928022`：government 类型，题目为社区便民服务站跨部门协同；附件 insufficient-evidence 夹具仅含材料撰写耗时、资料分散两条虚构记录，与题目不匹配。
- 16:21 direction:1 格式有效，但返回一个 evidence blocking、一个时间范围 warning；运行停在 `generated_invalid`，提示“阶段产物存在阻断问题”。已核对 runtime 的 classify_stage_issues 分支，无阻断时才自动推进；未盲重试或改运行 JSON。
- 此短流程仅证明识别资料不匹配并停止、未凑字数；未证明初步稿生成、后续阶段、DOCX 或 WPS 验收。输入不匹配是本次验收限制，不能直接外推所有资料不足流程失效。后续继续应先使用与题目相关的少量材料。
- Sol 调用返回 `Unknown model gpt-5.6-sol`，实际可用模型清单不含 Sol。依项目规范停在提交前，未暂存/commit/push，待模型可用或用户明确调整审核要求。
- 完整键盘路径、窄屏、axe 与视觉回归未验证。独立渲染器中文缺字输出不作为通过证据；WPS 检查为单独本机证据。

以下保留按时间形成的过程记录，失效状态以上述最新结果为准。

## 早期状态

未完成。来源为本仓库 main，基线 `1204d6e69112517132a94da5fa1910bad97a131d` 加当前未提交工作树。仅源码预览证据，未制包、安装或发布。

## 16 时续执行进展

- 修正根静态测试的旧脚本 URL 断言，保留主版本与专家团缓存版本替换检查，针对性测试通过。
- 完成新策略事实阻断工作稿出口：核验完整审核 ledger/回执身份，保留 blocked 等级与未解决意见；只放行明确的内容意见，工程身份/正文/缺章检查仍阻断。工作稿声明与意见绑定导出指纹并核验 DOCX 实际文本。
- 子代理有界验证：Python 三文件 69 passed，JS 模板合同 17 passed。主代理补充审阅意见显示与 HTML 转义，前端文件 96 passed。
- 同一源码实例重启后，真实工作台显示正文 12,126 字、超出目标篇幅、待复核工作稿及可展开的 5 项审阅意见。
- 企业任务通过产品“仅重新生成 DOCX”得到 attempt-3；canonical review:1 摘要仍为 `680939ea50646ff8357d52eabe58ef2b8a054807fd066144fda04d9861bfefc0`，未重新调用模型。DOCX SHA256：`bd26950b15e236b42e718c9ac5d725c1a2564042ebd75680ab0262f66131f4da`。
- WPS 明确打开 attempt-3/opened 下新版同名文件，检查封面声明、中段分析研判、末尾工作稿意见；中文正常、正文保留。旧版末尾空白页现承载工作稿说明，仍有较多留白，记录为 P2 排版限制。
- 产品已确认该工作稿本机交付，界面显示“研究已结束 · 文档已确认”；grade 仍 quality_review_required，不宣称篇幅达标或内容正式通过。
- 完整 `verify.sh --full` 已在本轮源码冻结后重新启动，结果待收集；政府与资料不足仍待执行。以下早期记录保留为过程证据，以本节更新为准。

## 目标与范围

依据 `docs/superpowers/plans/2026-09-08-research-v3-bounded-report-execution.md`：正式材料结构、标准篇幅、一次补写、一轮审核、可保存和导出的研究结果。影响专家团启动、运行状态、恢复与文档交付；不重做界面布局。

主内容为正文、实际篇幅和结果等级；辅助内容为原始诉求与审阅意见；内部身份及诊断放在详情。没有新增破坏性操作。

## 已实时验证的央国企场景

- session：`etl-fe5e3a1349c343acb0f0`。
- run：`et-ffecc76de33795f2d4b8545390771015f15bc65828dedfd7cd8701a26bd2f4e1`。
- 7/7 写作单元；一次长度补写；一轮独立分章审核。
- 正文 12,126 字，目标 8,000–12,000；超上限，`quality_review_required`，不能称篇幅达标或正式通过。
- 2026-09-08 15:19，产品“重新生成最终 DOCX”按钮恢复系统交付，run version 100，事件 `system_stage_completed`，错误字段清空。
- 25 项 DOCX 自动检查通过。产品“打开最终 DOCX”成功打开 WPS，显示中文正文和 17 页；完整首中尾核验与本机确认未完成。
- DOCX 相对实例 workspace 路径：`.taiji/expert-team-deliveries/<run>/delivery/attempt-2/delivery/document.docx`。
- 独立文档渲染器产出 11 页，但所检查页面中文缺字；此渲染结果不能作为排版通过证据。WPS 可显示中文，故不能将渲染器表现直接认定为 DOCX 内容丢失。

## 功能契约与状态

| 能力 / 普通用户入口 | 数据与反馈 | 验证 | 状态 |
|---|---|---|---|
| 新研究任务正式文体、类型、篇幅 | 已有启动 UI 与冻结合同 | 央国企实际运行；政府待验 | 部分验证 |
| 字数与结果等级 | 本轮补充 UI 显示 | 前端回归通过；重载后实际 UI 待验 | 未完成 |
| 系统交付失败恢复 | 可见重试按钮 | 实际进入文档确认 | 通过 |
| 保存并打开 DOCX | 产品可见文档按钮 | 实际打开 WPS | 通过 |
| 文档本机确认 | 可见确认按钮 | 首中尾未核完，未点击确认 | 未验证 |
| 事实阻断工作稿导出 | 仍被旧内容门禁拦截 | 独立代码检查确认 | 失败 |
| 政府标准报告 | 已有入口 | 尚未完成新策略完整流程 | 未验证 |
| 资料不足初步稿 | 已有夹具 | 本轮真实流程未执行 | 未验证 |

## 已修复

1. 已审非正式稿被正式等级门禁误拦；仅 `bounded_report_v1` 允许工作稿等级导出，历史无标记任务保留等级门禁。
2. 系统交付失败后的 generated_invalid 未被识别为冻结系统阶段，造成 resume 拒绝；已补 runtime/view，一次真实恢复成功。
3. 交付界面仍显示正在形成报告、统称正式 DOCX、缺实际篇幅；已调整并通过新增前端回归，实际重载待验。
4. v3 没有资料摘要投影时显示“本地资料 0”；改为不呈现未经核实的零值。
5. 旧 v2 自动推进夹具误用 v3 默认；冻结测试版本，显式 v3 测试保留。
6. runtime 审核完成后的非正式稿推进仅对新策略开放；历史无标记任务继续停在 generated_invalid，参数化回归覆盖两种策略。

## 自动化验证

本轮受影响的系统恢复、语义交付与前端测试三个文件：147 passed，1 条第三方 audioop 弃用警告。新增前端回归明确检查 12,126 字、超上限、待复核工作稿、研究已结束及不再误显示正式 DOCX。

`scripts/verify.sh --full` 退出 1：根测试阶段运行 1,334 项，1 项失败、2 项跳过。失败为 `test_packaged_webui_has_stable_version_and_agent_import_bootstrap`，其静态断言要求 commands.js URL 在 `__WEBUI_VERSION__` 后立即结束，与本轮新增 `etlaunch` 缓存版本参数不一致。尚未修正并复验，后续完整验证阶段不能视为通过。未以此前旧工作树 PASS 替代。

历史策略修复后的 runtime 与语义交付回归：38 passed，1 条第三方 audioop 弃用警告；`git diff --check` 通过。

## 视觉、可访问性与长时间工作

已看到 1263×768 源码 Electron 工作台和 WPS 正文截图；主操作有可读名称，工作台可滚动。AX 元素编号不稳定，截图坐标操作成功。修正后的新文案截图、完整键盘路径、窄屏布局、axe 自动化、视觉回归未验证。

加载/错误/交付待确认状态已实际查看；成功终态未验证。表单禁用和破坏性操作未重新测试，本轮未改相关操作。报告长文的首中尾本机检查未完成。

WPS 在检查途中被切换至另一 PPT，已向用户请求协调屏幕所有权，等待期间继续非 UI 工作。临时 caffeinate 已运行；因屏幕可能同时被操作，未继续无条件点击 WPS。

## 剩余问题与唯一推进顺序

- P1：blocked 结果仍无法导出带提示工作稿；必须保留 blocked 等级与实际意见，禁止直接把内容门禁全改通过。canonical、来源绑定与缺章等工程检查继续阻断。
- 待验：央国企文档确认、政府及资料不足两场景、修正后 UI。
- 必要门禁：完整本地验证、精确暂存后的 Sol 审核、commit/fetch/正常 push 尚未完成。

协调屏幕后先完成当前央国企确认；仅处理计划明确阻断，再依次政府/资料不足。不能另起单篇文采优化或手改运行 JSON。
