# 前端 UX QA 报告：专家团工作台点击外部空白区域关闭

## 状态

带限制完成。

本次交互的自动化合同、真实 Electron 主路径、截图和统一门禁均已通过。现有 Electron 长流程在本次独立验收完成后，仍会在旧的“发起专家团任务”入口等待超时；该失败发生在本次目标路径之外，未扩大范围修复。

## 变更范围

- 专家团 V3 右侧工作台的收起交互。
- 工作台关闭按钮与外部空白点击复用同一收起逻辑。
- 新增桌面壳/旧聊天壳空白背景合同测试和 Electron 鼠标验收。
- 未修改视觉样式、任务流程、接口、授权、数据结构或响应式断点。

## 主要用户目标

右侧工作台展开时，即使右上角关闭按钮被提示信息遮挡，也能点击旁边空白区域便捷收起，同时避免点击面板内容、聊天输入框或其他控件时误关闭。

## 主内容 / 辅助内容 / 高级内容

- 主内容：专家团工作台内的任务结果、修改意见和阶段操作。
- 辅助内容：工作台外的聊天/首页背景和已有恢复胶囊。
- 高级内容：无新增高级信息或调试入口。

## 已测试的主要用户路径

1. 1600×900 桌面宽屏打开专家团工作台。
2. 点击工作台标题和内部表单，确认不收起。
3. 点击工作台外的聊天输入框可见区域，确认不收起。
4. 用真实鼠标点击工作台左侧可见空白背景，确认立即收起。
5. 点击“展开专家团工作台”，确认工作台恢复且未提交草稿保留。
6. 点击原右上角关闭按钮，确认仍可收起，再次恢复正常。
7. 检查 1024px 既有全宽工作台合同，未新增遮罩或可点击空白层。

## 功能契约摘要

| 能力 | 数据/API/状态存在 | UI 入口存在 | 用户反馈存在 | 错误处理存在 | 空/加载/禁用状态 | 键盘/可访问性支持 | E2E/浏览器测试 | 状态 | 备注 |
|---|---:|---:|---:|---:|---:|---:|---:|---|---|
| 右上角按钮收起 | 是 | 是 | 是 | 不适用 | 不适用 | 是 | 是 | 通过 | 保留原 `aria-label` 和焦点行为 |
| 外部空白点击收起 | 是 | 是 | 是 | 不适用 | 窄屏不触发 | 鼠标增强，不替代按钮 | 是 | 通过 | 仅明确背景容器触发 |
| 内部/控件点击保护 | 是 | 不适用 | 不适用 | 不适用 | 不适用 | 是 | 是 | 通过 | 面板内部和输入框均不误关 |
| 草稿保留与恢复 | 是 | 是 | 是 | 是 | 不适用 | 是 | 是 | 通过 | 两种收起路径共用状态逻辑 |

## 真实浏览器测试证据

- 工具：项目既有 Playwright Electron 测试脚本。
- 目标路径输出：`EXPERT_TEAM_OUTSIDE_CLICK_SMOKE_PASSED`。
- 实际覆盖：内部点击、外部交互控件、外部空白鼠标点击、草稿恢复、关闭按钮、恢复入口。
- 隔离边界：独立 runtime、工作区、端口和测试授权夹具；未使用用户当前会话。
- 长流程限制：目标路径通过后，旧的“发起专家团任务”按钮等待超时，故完整长流程记为未验证，而不是通过。

## 截图情况

- 已检查收起态：`/tmp/taiji-expert-outside-click-20260827/00-outside-click-collapsed.png`。
- 已检查恢复态：`/tmp/taiji-expert-outside-click-20260827/00-outside-click-restored.png`。
- 收起态显示原有右侧“专家团”恢复胶囊；恢复态显示工作台、关闭按钮和保留的草稿，未发现布局错位。

## 可访问性检查

- 右上角关闭按钮保留可访问名称“收起专家团工作台”。
- 恢复按钮保留可访问名称“展开专家团工作台”。
- 外部空白点击仅作为鼠标便捷增强，不移除键盘可达按钮。
- 收起后焦点仍进入恢复按钮；恢复后焦点仍进入工作台标题。
- 自动化可访问性：未验证。原因：项目当前未在此路径配置 axe/Lighthouse，且未安装新依赖。

## 视觉层级检查

- 未新增遮罩、阴影、按钮、动画或视觉层级。
- 收起后沿用原恢复胶囊，展开后沿用原工作台视觉。
- 截图确认主内容、恢复入口和背景层级稳定。

## 长时间工作体验检查

- 减少移动鼠标到右上角的重复操作，尤其在提示信息遮挡时更易退出工作台。
- 不拦截聊天输入、工作台编辑和已有快捷卡片，避免工作过程被意外打断。
- 无新增动画、弹窗或高亮噪音。

## 空 / 加载 / 错误 / 成功 / 禁用 / 破坏性状态

- 空状态：真实空会话桌面壳已验证可找到空白背景并收起。
- 加载/错误/禁用：本次不改变这些状态；交互只在工作台已展开时生效。
- 成功反馈：工作台立即变为原有收起胶囊，反馈清楚。
- 破坏性操作：无；收起不丢弃任务或草稿，可立即恢复。

## 自动化检查运行结果

| 检查项 | 命令/工具 | 结果 | 备注 |
|---|---|---|---|
| TDD 聚焦回归 | `pytest ... -k 'outside_blank or workbench_binds'` | 通过 | 2 passed，两个 RED 均先因缺少对应覆盖而失败 |
| 专家团 V3 回归 | `pytest test_expert_team_frontend_v3.py`（排除两项既有环境/历史失败） | 通过 | 79 passed，2 deselected |
| JavaScript 语法 | `node --check` | 通过 | 生产脚本与 Electron 脚本 |
| WebUI 运行时 lint | `npm run lint:runtime` | 通过 | ESLint 0 错误 |
| Electron 目标路径 | `node tests/expert_team_v3_electron_smoke.js ...` | 通过 | 输出目标路径 PASS；后续旧长流程入口超时 |
| 项目统一门禁 | `scripts/verify.sh` | 通过 | verification: PASS；根测试 1295 通过、2 跳过，WebUI 690 通过 |
| Git 差异格式 | `git diff --check` | 通过 | 无空白错误 |

## 问题列表

| 严重程度 | 问题 | 证据 | 建议修复方式 | 是否已修复 |
|---|---|---|---|---|
| 无 | 本次范围未发现 P0/P1/P2/P3 问题 | 聚焦测试、Electron 目标路径、截图、统一门禁 | 不适用 | 不适用 |

## 已修复问题

- 关闭工作台不再依赖唯一的右上角按钮。
- 实现从旧聊天壳和当前太极桌面壳的明确空白背景收起。
- 避免把行为扩大为全局任意外部点击。
- 关闭按钮与外部空白点击不再维护两套状态逻辑。

## 剩余风险

- 若未来桌面壳更换背景容器类名，新容器需要显式加入允许列表；当前采用白名单是为防止误关。
- Electron 原有长流程的“发起专家团任务”入口超时尚未定位，发生在本次目标路径之后。

## 未验证项目

- 自动化可访问性未验证：未配置 axe/Lighthouse。
- 像素级视觉回归未验证：项目未配置截图基线比对。
- Electron 原有完整长流程未通过到结尾：本次独立目标路径已经通过，后续旧入口超时。

## 后续建议

本次无需追加功能。后续若单独治理 Electron 长流程稳定性，应另起任务定位“发起专家团任务”入口超时，避免与本次交互修改混合。
