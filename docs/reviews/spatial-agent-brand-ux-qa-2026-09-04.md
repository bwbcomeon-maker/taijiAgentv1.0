# 前端 UX QA 报告：国网空天智能体品牌展示

## 状态、范围与用户契约

带限制完成：源码桌面展示、聚焦测试及分级门禁所选项目通过，真实安装态未验证。来源：正式仓库 main，基线 `bc978a9d` 加本次工作树。用户确认仅考虑桌面、不做窄屏专项。

- Logo 下主标题：国网空天智能体；副标题：空间数据运检智能体。
- 对话默认智能体显示名：Agent。兼容旧 `taiji Agent` / `taijiAgent`、大小写变体及原 Hermes 默认别名；用户自定义名称和非默认专家名称不强制改写。
- 关于页：主标题国网空天智能体；第二行空间数据运检智能体 + 桌面版标签；原 Logo、版本及版权不变。
- 同一主工作区顶部品牌胶囊同步新产品名，输入框提示同步 Agent。
- 非目标：安装包标识、系统窗口名、系统提示词、历史消息正文、版权归属、运行配置迁移、制包及安装。

## 功能契约表

| 能力 / 契约 | 用户与入口 | 状态 / API | UI / 反馈 | 错误 / 加载 / 禁用 | 键盘 / a11y | 浏览器 | 结果 |
|---|---|---|---|---|---|---|---|
| 侧栏品牌及顶部产品标识 | 桌面用户 / 全局侧栏与聊天 | 静态展示 | 主副标题层级 | 不新增异步状态 | 无新增操作 | 1440×1000 | 通过 |
| 对话默认署名与输入框 | 桌面用户 / 对话 | 统一显示名函数，兼容旧配置 | Agent 署名可见、输入框同步 | 空名称回退 Agent | 原输入控件保留 | 旧默认、空名称、自定义名、历史正文 | 通过 |
| 关于页品牌层级 | 桌面用户 / 设置→关于 | 关于 API 契约不变 | 产品名、副标题、桌面版标签 | 原说明加载保留 | 无新增交互 | 可见且无横向溢出 | 通过 |

## 实施与验证

- 生产文件：`static/index.html`、`static/ui.js`、`static/boot.js`、`static/style.css`。未改写用户配置或历史消息。
- RED：新品牌回归在旧页面上 2 失败、7 通过，失败对应侧栏和关于页旧名称。
- GREEN：`test_settings_about_panel.py` 与 `test_spatial_brand_display.py` 共 9 项通过；保留关于 API 固定版本/版权合同。
- 新隔离 `spatial_brand_browser_smoke.py`：静态服务、mock API、外部请求阻断、WebSocket 关闭、独立无头 Chromium；没有真实 Provider 或用户状态访问。
- 浏览器断言覆盖旧默认名称归一化、自定义名保留、对话署名文本及可见性、输入提示、历史正文不变、侧栏与关于页文本及横向溢出；退出 0。
- 日志 `/private/tmp/spatial-brand-browser.log`；最终截图 `/var/folders/5h/f2nd43h57ll1gn08pqvv722c0000gn/T/spatial-brand-browser-7oimx1_g/chat-desktop.png` 与 `about-desktop.png`，均已人工查看。显式启用桌面样式标记，仅是源码样式验证，不等于真实 Electron/Kylin 安装态。
- 分级验证首轮日志 `/private/tmp/spatial-brand-verify.log`：根测试 1321 项通过（跳过 2 项），WebUI lint 通过；WebUI 950 通过、2 条旧名称断言失败，因此首次统一入口退出 1，不宣称首次全绿。
- 两条失败要求旧默认 `taiji Agent` 和旧输入框中文转换，与新需求冲突；仅更新 `test_issue1116_composer_placeholder.py` 的两条文案合同，不改生产代码迎合测试。按 `verify.sh --plan` 列出的 WebUI 集合重新运行，并合并本次新测试及后续 bootstrap/coexistence 集合，1036 项全部通过，日志 `/private/tmp/spatial-brand-webui-recheck.log`；Agent 品牌与 bootstrap 剩余集合通过，日志 `/private/tmp/spatial-brand-agent-recheck.log`。最终 `git diff --check` 与完整本地变更安全扫描通过。没有再次运行整个统一入口，不复用上一任务门禁结论。

## 视觉、可访问性与持续使用

侧栏主标题 24px 加粗，副标题沿用较小弱化文字；关于页主标题沿用 28–34px，第二行 17px，桌面版为 12px 小标签。保留布局和间距体系，无挤压或遮挡。消息署名 Agent 与原头像对齐，品牌名与对话角色名区分明确。没有新增表单或弹窗，不改变焦点、键盘顺序与操作路径；未新增颜色唯一传达状态的设计。没有开展长时间使用测评。

## 问题与未验证边界

当前未发现受影响桌面路径的 P0/P1。窄屏按用户要求不纳入此次验收；axe、像素视觉回归、真实模型生成内容及真实安装态未验证。新名称只作用于 UI 默认署名，不追溯替换消息正文中已有的名称，也不声称限制模型如何自称。
