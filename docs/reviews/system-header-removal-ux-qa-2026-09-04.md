# 前端 UX QA 报告：系统页头部精简

## 状态

带限制完成。证据为源码与隔离浏览器，不代表麒麟安装态验收。

## 变更范围与主要用户目标

基于 main `78a6fe047ced75dd8d42b51a5f7d4f09265263ce`，移除用户截图红框中的系统标题、说明、WebUI/Agent 构建版本和立即检查更新区域。仅删除 HTML 头部，更新版本 DOM 合同及现有浏览器 smoke；不修改版本 API、后台更新逻辑、配置、运行检查或支持包处理逻辑。

## 主内容 / 辅助内容 / 高级内容

运行检查成为系统页首项；安全与访问设置保留在后续区域。低频构建元数据不再挤压标题造成逐字折行；当前上下文仍由设置导航的“系统”选中项提供。

## 已测试的主要用户路径与功能契约摘要

| 能力及依据 | 角色 / 入口 | 数据与状态 | UI / 反馈 / 错误处理 | 状态覆盖 | 可访问性 | 浏览器证据 | 结论 |
|---|---|---|---|---|---|---|---|
| 用户明确要求去掉红框 | 普通用户 / 系统页 | 不更改 API | 头部 DOM 删除，无额外反馈需要 | 静态展示 | 删除区无残留可聚焦控件 | 版本区不存在，诊断卡为首子元素 | 通过 |
| 保留运行检查入口 | 普通用户 / 系统页 | 原处理逻辑未改 | 重新检查及导出按钮可见；操作反馈未重验 | mock 待确认状态已看；其他状态未重验 | 原生文字按钮保留 | 两按钮可见断言 | 入口通过，业务操作未验证 |

## 真实浏览器测试证据与截图

执行 `tests/spatial_brand_browser_smoke.py`，系统 Python 3.13 + 现有 Playwright、隔离静态服务器、mock API、拦截外部请求；1440×1000 桌面视口。脚本退出 0，页面错误断言通过，同时保留品牌、聊天名称和关于页回归。

- 日志：`/private/tmp/system-header-browser.log`
- 截图：`/var/folders/5h/f2nd43h57ll1gn08pqvv722c0000gn/T/spatial-brand-browser-sa4s7mx8/system-desktop.png`
- 已人工查看截图：红框区域与占位消失，运行检查上移，操作按钮可见。
- 系统页由现有页面函数打开；未把此断言称为真实导航点击或诊断业务验收。

## 可访问性、视觉层级与长时间工作体验

人工语义检查：诊断区保留 `aria-labelledby` 和有文字的原生按钮；删除区不留隐藏焦点目标。未重验完整 Tab 顺序、屏幕阅读器或焦点外观，自动化可访问性未验证。截图确认主内容突出、无原先的标题逐字折行和顶部大块空白；无新增动画或高亮。长时间体验仅作静态检查，未进行持续使用测试。

## 空 / 加载 / 错误 / 成功 / 禁用 / 破坏性状态

本次删除静态头部，不新增状态或数据变更。mock 待确认和只读显示已观察；真实诊断加载、成功、错误、导出及安全配置保存未验证，不宣称通过。无新增破坏性操作。

## 自动化检查运行结果

| 检查项 | 命令/工具 | 结果 | 备注 |
|---|---|---|---|
| RED | pytest 新增头部删除合同 | 1 failed，29 deselected | 修改 HTML 前，旧版本 DOM 存在导致预期失败 |
| GREEN | pytest tests/test_version_badge.py | 30 passed | 绑定项目 Agent 解释器与目录 |
| 浏览器 | spatial_brand_browser_smoke.py | PASS，退出 0 | 含系统页新增断言与截图 |
| 分级回归 | scripts/verify.sh | PASS，退出 0 | 日志 `/private/tmp/system-header-verify.log`；使用已准备的 Node 24 |
| 补丁格式 | git diff --check | PASS | 无空白错误 |

## 问题列表与已修复问题

| 严重程度 | 问题 | 证据 | 建议修复方式 | 是否已修复 |
|---|---|---|---|---|
| P2 | 冗长构建版本挤压系统页头部，影响主内容 | 用户截图与浏览器复核 | 按要求删除整个头部展示 | 是 |

未发现本次删除引入的 P0/P1。另观察到既有安全说明仍有“正式安装需要使用企业安全”文字，与此前允许本机调试的策略存在文案不一致；本次仅处理红框，不改变该规则或说明。

## 剩余风险、未验证项目与后续建议

- 麒麟/Electron 安装态、制包、安装：未执行。
- 像素级视觉回归、axe 自动化、窄屏：未验证；本次按桌面范围实施，不安装新依赖。
- 真实诊断请求、导出支持包、检查更新业务：未验证；版本 API 和后台更新代码未改。
- 如需交付到麒麟，应另行启动绑定新提交的制包流程；安全说明的既有不一致可单独修正。
