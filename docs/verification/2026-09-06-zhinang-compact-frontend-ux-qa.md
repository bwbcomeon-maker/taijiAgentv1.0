# 前端 UX QA 报告：智囊库紧凑布局

## 范围与来源

用户已确认分类展开三列／收起四列设计稿。本次只调整智囊库页面的卡片结构、页头、搜索工具栏和模块作用域内的侧栏尺寸。保持角色数据、图片清单、筛选与创建任务 API 不变。

基线 `main@bc293bd83a1e040143983cc0c5638dad2defcea9`，根目录与 Git common dir 已核对，开始时工作树干净、远端差异 `0 0`。主 Agent 唯一写入，Sol 只读审核。下述证据绑定本提交的工作树内容；提交与推送状态以最终 Git 记录为准。

## 问题与改动

原布局固定两列、卡片最小高 220px；全局非聊天页头的 70–92px 顶部 padding 与宽分类栏进一步挤占列表。搜索位于可收起的分类栏中。

现布局使用结果区 container query，宽屏三／四列、窄屏自动减列。卡片实色白底、图标与标题并排、说明两行、标签与详情紧凑排列；长标题允许增高。搜索保留原 ID 与事件，移到主工具栏。分页在网格滚动区外，不覆盖卡片。共享 shell 尺寸仅在 `data-active-panel="zhinang"` 时改变。

## 功能契约与证据

契约依据：已确认设计稿、`docs/requirements/2026-09-05-taiji-zhinang-prd.md` 和智囊库用户说明；目标角色为普通智囊库使用者。

| 能力 / 入口 | 数据与反馈 | 状态及错误处理 | 键盘与可访问性 | 浏览器证据 / 结论 |
|---|---|---|---|---|
| 目录、卡片、响应式网格 | 真实 catalog HTTP 与数量提示 | 保留 skeleton、空态、error；目录失败与重试已操作 | 标题语义保留、详情及收藏为原生按钮 | 七尺寸无横向溢出；通过 |
| 搜索 / 主工具栏 | 原 query 与 200ms 防抖，真实结果匹配 | 无匹配显示清除筛选，恢复全部目录 | 唯一 input ID 与关联 label 保留 | 展开、收起与窄屏可见且可搜索；通过 |
| 分类 / 分类栏 | 原 category 与结果重绘 | 清除条件与全部角色入口保留 | 原按钮 aria-pressed 保留 | 设计与体验类别结果核对；通过 |
| 收藏 / 卡片及我的收藏 | 原 favorites HTTP，保存后状态可见 | pending 与错误逻辑未改；本轮保存成功已验证，收藏失败未重新注入 | Enter 收藏成功；aria-pressed 状态核对 | 收藏后我的收藏可找到角色；通过（失败分支未验证） |
| 详情 / 查看详情 | 原详情 HTTP 与创建入口 | 详情错误分支未重新注入 | Escape 关闭并恢复原详情按钮焦点 | 七尺寸打开与关闭，通过；创建执行未验证 |
| 分页 / 网格下方 | 原页码与每页24条 | 首页上一页禁用 | 原生按钮，有分页 nav 名称 | 下一页24条，分页顶部与网格底部相接；通过 |
| 最近使用 / 视图工具栏 | 原 view=recent | 空态浏览全部入口可用 | 原生按钮 | 空态与恢复通过；历史任务继续执行未验证 |
| 相邻聊天页面 | 既有导航与宽度 | 不改会话数据 | 原导航 | 返回聊天后品牌栏恢复原尺寸；通过 |

## 已实时验证

`ZHINANG_E2E_SCOPE=compact-layout` 使用既有隔离服务 harness，新增检查模块 `tests/zhinang_compact_layout.cjs`。完成 **115 项检查，0 次 Provider 请求，0 个 page error**。

| CSS 视口 | 展开列数 | 收起列数 | 首卡尺寸（约） |
|---|---:|---:|---|
| 1440×900 | 3 | 4 | 展开311×176；收起281×176 |
| 1280×800 | 2 | 3 | 展开340×176；收起327×176 |
| 1024×768 | 2 | 2 | 展开266×176；收起340×176 |
| 768×1024 | 1 | 不适用 | 484×176，单列填满内容宽度 |
| 390×844 | 1 | 不适用 | 362×176，移动端填满内容宽度 |
| 650×800 | 1 | 不适用 | 366×176，验证手机断点上方 |
| 720×450 | 1 | 不适用 | 436×176，等效1440×900的200% reflow视口 |

720×450 是缩小 CSS 视口的 reflow 检查，并未操作浏览器真实缩放菜单。网格允许纵向滚动；720×450 已验证页头保持固定、首张卡片与分页完整可见。641–900px 分类栏收紧为192px，64px导航栏独立滚动并常显文字标签，避免悬浮提示裁剪；外层 body 不产生滚动偏移。

环境：Node v24.19.0、Python 3.11.15、Chromium 147.0.7727.15。Python 固定仓库 `hermes-agent/venv/bin/python`，WebUI 固定当前 `server.py`；`HERMES_WEBUI_AGENT_DIR` 指向同仓库 Agent。`HERMES_HOME`、state、workspace、配置均为本次临时隔离目录。仅监听 loopback，浏览器拦截外网，未打开默认浏览器。测试结束停止服务与 Chromium。

证据位于 `/private/tmp/zhinang-compact-final-evidence/e2e-evidence-compact-layout.json` 及同目录截图；精选截图已另存本任务可视化目录。修改前截图与固定两列 RED 位于 `/private/tmp/zhinang-compact-red/`。中途样式修正与测试等待条件纠正不冒充业务成功：收起状态 CSS 优先级问题已修复，搜索断言按真实多字段匹配规则核对，导航按实际可见入口选择。

现有 `tests/test_zhinang_ui.py`：**10 passed**。首次独立 pytest 因缺少显式 Agent 路径而加载错误来源，补齐与统一脚本一致的 Agent/Python 环境后通过；没有修改业务代码规避该错误。

现有 `zhinang_frontend_runtime_contract.cjs` 显式传入当前 `static/sessions.js`：**10 passed / 0 failed**，覆盖草稿保存与会话切换合同；这不是实际创建任务验收。

变更说明保存在 `docs/changes/2026-09-06-zhinang-compact-layout.md`。组件要求更新 CHANGELOG.md，但该文件已有约1.19 MB，与安全脚本非源码1 MiB门禁冲突；只撤回本轮新增段落，保留独立说明，不修改门禁。

## 统一验证与审核修正复验

统一 `scripts/verify.sh` 已输出 `verification: PASS`：根目录1334项（跳过2项）、WebUI953项以及其余选定子套件通过，日志 `/private/tmp/zhinang-compact-verify.log`。其后 Sol 预审提出窄屏与证据覆盖意见，最后修改仅涉及 CSS 与 compact 浏览器检查模块，已重新执行受影响的浏览器检查（115项）、UI合同（10项）及本地安全/差异检查，没有扩大重复无关根目录测试。

最终 E2E JSON 的 `compactSourceBefore` 与 `compactSourceAfter` 完整记录 index.html、zhinang.css、zhinang.js 和两个浏览器测试文件 SHA-256，并断言测试前后相同。最终暂存审核须再次核对这些摘要与当前内容一致。

Sol 预审意见均已处理：641–900px窄侧栏、aside减列与无溢出、窄屏再次打开分类/收藏、源码摘要绑定。额外截图复查定位 body.scrollTop=85 导致页头top=-85；限定智囊窄屏 `overflow:clip` 并让 rail 独立滚动后，页头top和body.scrollTop均为0，新增断言保护。

## 未验证边界

- 自动化 axe / 对比度扫描、像素级视觉回归：未验证；本轮使用浏览器几何断言及截图人工复查。
- 完整角色任务创建与继续、真实 Provider、真实 Electron、安装态和目标机：未验证。
- 加载 skeleton、收藏失败、详情失败与历史不可用角色保留既有逻辑；未对这些分支重新逐项注入故障。
- Sol 最终结论绑定精确暂存内容，见任务最终审核记录，不能以本台账代替审核。
