# 太极 Agent 国产终端安装包交付目录

最终 DEB/RPM/RUN 产物放在本目录。

当前源码提供 DEB 构建脚本：

```bash
TAIJI_AGENT_VERSION=0.1.0 ./packaging/linux/deb/build-deb.sh
```

注意：最终安装包必须在 Linux x86_64/amd64 构建并在海光 + 统信/UOS 目标机验证后，才能标记为“目标机已验证”。
