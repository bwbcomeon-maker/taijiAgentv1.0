# Taiji Agent 项目规则

## 1. 适用范围与规则来源

- 当前明确任务边界及有效项目规则优先于旧记忆；旧记忆不能覆盖当前授权或作为现状证明。仅分析、审查或制定计划不触发文件修改或 commit/push。
- 涉及仓库修改、Git/GitHub、审查/CI、源码启动、运行目录、持久服务、制包或发布前，完整阅读并遵循[开发生命周期规范](docs/runbooks/development-lifecycle.md)。它统一授权、流程和证据口径；辅助手册不得另设冲突规则，本文件不重复详细命令。
- 使用下文明确链接的项目内 Skill；同名全局 Skill 不得替代项目指定版本。规则与实际脚本不一致时报告差异，不绕过门禁，也不把文档承诺当作已实现能力。

## 2. 来源核对、证据和问题处理

- 修改前核对物理仓库根、Git common dir、分支、worktree 清单、HEAD 和工作树状态；启动或验收时再绑定源码入口、解释器、进程及 runtime/config 来源。
- 优先复现问题、定位根因并说明影响；证据不足时明确写“根因未确认”。同一故障假设最多两次诊断或修复，仍失败则停止该路径、回退试探性改动并保留证据，不为满足结论要求而推测。
- 按当前内容和环境标注 `已实时验证`、`未实时验证`、`历史线索`。没有实际验证，不得宣称已完成或已修复；源码、制品、安装态和发布态证据不得互相替代。
- 自动化和常规调试使用隔离配置、mock 与测试资料，不打开默认浏览器或真实 OAuth/Provider 页面；真实验收按明确授权范围执行。
- 修复报告说明修改、验证、剩余风险和下一步。接口、流程、支持范围或门禁变化时更新手册；当轮执行结果进入验证台账，历史日志不堆入本入口。

## 3. 日常开发与标准收尾

- 日常直接在 `main` 开发；`main` 是日常开发主线，不等同于稳定版本。验证结论必须绑定具体提交或明确的工作树内容，分支名不构成验证证据。
- 同一共享工作树及 Git index 同一时间只有一个协调后的写入者，覆盖跨任务场景；其他参与者保持只读。已有修改先确认归属，不覆盖、回退或夹带；分支/worktree 例外按生命周期规范另行授权。
- 用户要求修改、修复或完成仓库任务且未明确限定 `local-only` 时，默认完成本地验证、精确暂存、最终 Sol 审核、commit、刷新远端及正常 push `main`，无需再次询问；更具体的停止边界优先。“按标准收尾”是同一默认流程的快捷语义，不是额外授权门槛。
- commit 前的 Sol 最终审核必须绑定完整暂存内容，详细视图见[单人开发手册](docs/runbooks/solo-development-workflow.md)。任何 staged bytes 变化都使旧审核失效并要求重新审核；达到修复轮次上限仍有阻断项时停止提交。无法获得 Sol 审核时停在提交前；不规定必须由 Luna/Spark 执行。
- `Main Validation` 是 push 后异步、非强制的补充证据，不能替代本地验证和最终审核，也不能替代发布链要求的成功 CI 证据。
- 普通收尾不授权 Tag、Release、制包、签名、安装、部署、持久服务或发布；禁止强推、共享历史改写、影响其他任务或删除归属不明的内容。

## 4. 制包、安装和发布入口

- RC 使用不可移动的 `vX.Y.Z-rc.N` 附注标签；稳定版本使用不可移动的 `vX.Y.Z` 附注标签；GitHub Release 只能绑定正式稳定标签。创建/推送 Tag、Release 及后续交付动作分别核对明确授权，不移动已有版本标签。
- Linux/Kylin/UOS 制包、安装与运行链、离线交付及用户安装包去 Hermes 化，使用项目内 [$taiji-kylin-packaging](.agents/skills/taiji-kylin-packaging/SKILL.md) 和[交付手册](docs/runbooks/taiji-kylin-uos-offline-delivery.md)。候选 DEB 使用 `taiji-package`；正式离线演练、认证、签名和客户发布使用黄金编排器，候选状态不能冒充正式 checkpoint。
- 当前 Linux 输入链仅接受已复验的 clean `main`；Linux hotfix 分支制包尚未支持，通用发布检查支持 hotfix 不代表 Linux 制包链可用。
- 真实代表主机通过 `kylin` 别名访问。项目已允许的只读 SSH 检查无需重复确认；传输、安装依赖、构建、安装验收、签名和发布分别核对有效授权。相同对象、阶段和影响范围内的授权可以延续，对象或摘要变化时重新核对范围；保留控制器的 `BUILD` 确认机制。
- 项目文件不得写入主机固定 IP、密码、私钥或个人凭据；不得关闭或放宽 Kysec/安全策略、替换系统核心库或扩大无关系统权限。主机操作、传输和首次安装基线检查详见交付手册。
- 部分安装/桌面检查仅作局部证据；“目标机已验证”须满足交付手册的完整业务验收，不能外推到其他环境或完整支持矩阵认证。
- Windows 候选使用[独立目标流程](docs/runbooks/taiji-windows-candidate-pipeline.md)，不套用 Linux transport；支持设计和静态合同不等于真实制品或真机验收通过。

## 5. 前端 QA 适用条件

- 实际实施、修改、审查或验收用户可见界面、交互、可访问性、浏览器或视觉效果时，使用项目内 [$frontend-ux-qa](.agents/skills/frontend-ux-qa/SKILL.md)。纯规则文档审查不触发整套产品 UI QA；创造性 UI 变更仍先使用 brainstorming。
- UI 入口按已确认的产品功能契约和授权角色检查。承诺通过界面提供的能力缺少可发现、可访问入口，至少为 P1；主流程阻塞为 P0。内部 API、明确 CLI-only 或角色之外的操作不因缺少普通用户 UI 自动判为缺陷；既有产品回归不得借范围划分排除。
- 前端任务完成前输出中文《前端 UX QA 报告》，围绕受影响路径提供证据。未执行的浏览器、截图、可访问性自动化和视觉回归标注“未验证”，不得仅凭编译或代码阅读判为通过。
- 分支/worktree 中的界面效果只能证明该来源；正式 `main` 的源码验证也不能替代安装态或目标机证据。
